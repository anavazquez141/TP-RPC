server:
  port: 8080
spring:
  cloud:
    gateway:
      routes:
        - id: grpc-service
          uri: http://localhost:9090
          predicates:
            - Path=/grpc/**
          filters:
            - StripPrefix=1