/*package RpcDonaciones.repositories;

import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;
import RpcDonaciones.entities.AdhesionEvento;
import java.util.List;

@Repository
public interface IAdhesionEventoRepository extends JpaRepository<AdhesionEvento, Long> {
    List<AdhesionEvento> findByIdEvento(String idEvento);
    List<AdhesionEvento> findByIdOrganizacion(String idOrganizacion);
    List<AdhesionEvento> findByIdVoluntario(String idVoluntario);
}
*/